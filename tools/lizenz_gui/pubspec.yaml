name: lizenz_gui
description: "Zeitexa Lizenz-Generator (GUI) - nur fuer den Entwickler-Rechner, wird NICHT an Firmen verteilt."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  bcrypt: ^1.2.0
  # WICHTIG: bei ^10 belassen (siehe Projektregeln der Haupt-App).
  file_picker: ^10.0.0
  lizenz_shared:
    path: ../../packages/lizenz_shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
